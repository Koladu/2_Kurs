#include "Header.h"

void chtenie(ORDER order[N]) 
{

	std::ofstream fout;          // поток для записи
	fout.open("D:\\Order.txt"); // окрываем файл для записи

	if (fout.is_open())
	{
		fout << "\t\t\t\t\tПорядок" << endl;
		fout << "Расчетный счет плательшика\tРасчетный счет получателя\tПеречисленная сумма\n";
		for (int i = 0; i < N; i++) 
		{
			fout << "\t" << order[i].Platelchik << "\t\t\t" << order[i].Poluchatel << "\t\t\t" << order[i].Summa << endl;
		}
	}
	fout.close();

	std::string line;
	std::ifstream in("D:\\Order.txt"); // окрываем файл для чтения
	if (in.is_open())
	{
		while (getline(in, line))
		{
			std::cout << line << std::endl;
		}
		
	}
	in.close(); vk;   // закрываем файл
}